{
    "name": "sisters-station",
    "version": "1.0.0",
    "description": "Multi-vendor baby wear e-commerce marketplace",
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "vite",
        "build": "npx vite build",
        "postinstall": "chmod +x node_modules/.bin/vite",
        "vercel-build": "npm ci && npm run build"
    },
    "devDependencies": {
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    },
    "dependencies": {
        "bootstrap": "^5.3.0",
        "bootstrap-icons": "^1.11.0"
    }
}
